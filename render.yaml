services:
  - type: web
    name: SIH_final_backend
    env: python
    plan: free
    rootDir: src/api
    buildCommand: pip install -r ../../requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port \
